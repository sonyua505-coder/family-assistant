{
  "api_base": {
    "description": "家庭信息助理 API 地址",
    "type": "string",
    "default": "http://homeassistant-api:3000",
    "hint": "容器内经共享网络访问，一般保持默认"
  },
  "api_key": {
    "description": "X_API_KEY 访问密钥",
    "type": "string",
    "hint": "见 /opt/homeassistant/.env 的 X_API_KEY，勿外泄"
  },
  "poll_interval": {
    "description": "outbox 轮询间隔（秒）",
    "type": "int",
    "default": 15,
    "hint": "范围 5-120"
  },
  "disable_legacy_query_tools": {
    "description": "停用旧查询/统计 LLM 工具（list_bills / get_bill / query_bill_stats / query_bill_changes / list_bill_trash / restore_bill），验证新工具 bill_stats / export_bills",
    "type": "bool",
    "default": false,
    "hint": "验证新工具期间设 true，验证完可改回 false 恢复旧工具"
  }
}
