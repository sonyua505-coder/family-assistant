# 依赖与构建产物
node_modules/
dist/

# 环境变量（铁律：.env 不提交，X_API_KEY 只存在于服务器 .env）
.env
.env.local

# 数据与备份（SQLite 文件、冷备）
data/
backup/
*.db
*.db-shm
*.db-wal

# 日志
*.log
logs/

# Python（AstrBot 插件）
__pycache__/
*.pyc

# 部署配置（本地不跟踪、不执行，由云端自持；磁盘保留作参考）
Dockerfile
docker-compose.yml
.dockerignore
deploy/

# WorkBuddy 项目内部数据（记忆/会话产物，不随代码库分发）
.workbuddy/

# 内部文档（本地自持，不随公开仓库分发；历史提交中仍可见）
docs/下一阶段开发方向.md
docs/云端接入与维护手册.md
docs/项目解析.md
docs/BOT工作区机制.md
