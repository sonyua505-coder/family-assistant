# 依赖与构建产物
node_modules/
dist/

# 环境变量（铁律：.env 不提交，X_API_KEY 只存在于服务器 .env）
.env
.env.local

# 数据与备份（SQLite 文件、冷备）
data/
backup/
*.db
*.db-shm
*.db-wal

# 日志
*.log
logs/

# Python（AstrBot 插件）
__pycache__/
*.pyc

# 部署配置（本地不跟踪、不执行，由云端自持；磁盘保留作参考）
Dockerfile
docker-compose.yml
.dockerignore
deploy/
